# 이 파일을 .env 로 복사한 후, 각 환경에 맞는 실제 값으로 채워주세요.
# Django 설정
SECRET_KEY=your_django_secret_key
DEBUG=True

# Redis 설정 (Docker Compose 네트워크)
# docker-compose.yml 에 정의된 서비스 이름(redis)을 사용합니다.
REDIS_HOST=redis
REDIS_PORT=6379

# 데이터베이스 설정 (Docker Compose 사용 시)
# docker-compose.yml 에 정의된 서비스 이름(db)과 변수를 사용합니다.
POSTGRES_DB=your_db_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
DB_HOST=db
DB_PORT=5432
